# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node
node_modules/
npm-debug.log*

# Logs and local env
*.log
.env
.env.*

# NOTE: there is deliberately no `.task/` entry here.
# Orchestration state lives outside the repository entirely, in
# $XDG_STATE_HOME/agent-delegation/ — see workflows/default/references/task-dir.md.
# None of this system's
# state reaches the working tree, so there is nothing to ignore. A companion
# skill that writes its own directory excludes it itself; that is not ours.

# Orchestrator worktrees (created as siblings of the repo, but just in case)
.adg-worktrees/

# Local agent/editor working docs (specs, plans, scratch) — never part of the repo
.claude/
.DS_Store
